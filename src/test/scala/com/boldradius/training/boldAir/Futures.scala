
package com.boldradius.training.boldAir

import org.scalatest.FreeSpec

class Futures extends FreeSpec {
  // Use Futures
  // compute an actual elapsed time for flights, with each future waiting for the flight to complete
}

